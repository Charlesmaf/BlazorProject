@if (IsLoading)
{
    <div class="loading-overlay">
        <div class="loading-spinner">
            @* You can replace this with any other loading indicator (e.g., a custom SVG, Lottie animation) *@
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
        </div>
    </div>
}

@code {
    [Parameter]
    public bool IsLoading { get; set; } = false;
}
<style>
    .loading-overlay {
        /* position: fixed;  */
        /* top: 0; */
        /* left: 0; */
        /* width: 100%; */
        /* height: 100%; */
        /* background-color: rgba(0, 0, 0, 0.5);  */
        /* display: flex; */
        /* justify-content: center; */
        /* align-items: center; */
        /* z-index: 1000;  */
    }

    .loading-spinner {
        /* No need to set position here, as it will be centered by the flexbox properties of .loading-overlay */
        /* You can adjust the size of the spinner here, if needed */
        /* Example: */
        /* width: 5rem;
            height: 5rem; */
    }
</style>